.container
  h1 {{item.title}}
  .row
    .col-md-6
      h3(ng-show="item.featured") Featured
      h3 Published on {{item.published | date}}

    .col-md-6
      .panel.panel-primary
        .panel-heading Tags
        .panel-body
          div(ng-repeat="tag in item.tags") {{tag}}